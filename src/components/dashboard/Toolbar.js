import React from "react";

import "./toolbar.scss";

// Toolbar
// Fully-customizable toolbar that sits above the Board component
export const Toolbar = () => {
    return <div className="toolbar">Toolbar</div>;
};
